# Tasks — item-create

- [ ] Add an item model + repository in `src/app` with create + lookup-by-id
- [ ] Reject empty names before persisting
- [ ] Add unit tests underneath the property oracle `ORA-ITEMC-1a`
- [ ] Run the full local suite and confirm green
