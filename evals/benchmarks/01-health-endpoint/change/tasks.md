# Tasks — health-endpoint

- [ ] Add a `GET /health` handler in `src/app` returning HTTP 200 and `{"status":"ok"}`
- [ ] Add a unit test underneath the contract oracle `ORA-HEALTH-1a`
- [ ] Run the full local suite and confirm green
