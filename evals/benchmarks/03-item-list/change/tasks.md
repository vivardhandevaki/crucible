# Tasks — item-list

- [ ] Add a `list()` operation in `src/app` returning items in insertion order
- [ ] Guarantee no duplicates in the returned list
- [ ] Add unit tests underneath the property oracle `ORA-ITEML-1a`
- [ ] Run the full local suite and confirm green
