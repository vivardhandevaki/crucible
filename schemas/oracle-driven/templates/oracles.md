# Oracle Map — <!-- change-name -->

<!--
  Every SHALL/MUST in this change's spec deltas MUST have at least one row below.
  Oracle Type ∈ { property | example | contract | db-constraint | archunit | ci-check | human-audit }.
  Oracle ID format: ORA-<CHANGE>-<n><letter> (stable, unique). e.g. ORA-PC-2a
  Status: DRAFT → IMPLEMENTED → APPROVED (APPROVED = merged under /oracles on the default branch).
  Implementation Path points at the file under /oracles/** (use "n/a" only for human-audit).
  Prefer property over example whenever the requirement quantifies over inputs.
-->

## Traceability Table

| REQ ID | Requirement (verbatim SHALL/MUST) | Oracle ID(s) | Oracle Type | Implementation Path | Status |
|--------|-----------------------------------|--------------|-------------|---------------------|--------|
| <!-- REQ-<CHANGE>-1 --> | <!-- verbatim requirement line --> | <!-- ORA-<CHANGE>-1a --> | <!-- property --> | <!-- oracles/properties/... --> | DRAFT |

## Unmapped / Downgraded Requirements

<!--
  List any requirement NOT covered by an executable oracle, each with its disposition:
  sharpened / downgraded-to-guidance / human-audit (+ written justification).
  Leave this section empty if every requirement has an executable oracle.
-->
