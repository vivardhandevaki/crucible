package app;

/**
 * Scaffold placeholder so the build is green from commit zero. Replace with
 * real modules (packages: app.api / app.service / app.domain / app.persistence
 * — the architecture oracles key on these names).
 */
public final class Placeholder {

  private Placeholder() {}

  /** The scaffold's only behavior; deleted with the first real feature. */
  public static String status() {
    return "crucible-governed";
  }
}
